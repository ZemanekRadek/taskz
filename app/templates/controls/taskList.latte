<div class="task-block">
{foreach \App\Model\Helper::listDay($List->getTasks()) as $block}
<div class="task-block-day">
	<div class="task-block-day-title">
		<div class="task-block-day-title-name">{$block[day]}</div>
		<div class="task-block-day-title-counter">{count($block[items])}</div>
	</div>
	<div class="task-block-day-items">
	{foreach $block[items] as $item}
		<div class="task-item">
			{$item->name}
		</div>
	{/foreach}
	</div>
</div>
{/foreach}
</div>
